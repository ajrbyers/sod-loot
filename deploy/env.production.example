# Copy to /opt/sod-loot/.env and fill in. Keep this file OUT of git.
WCL_CLIENT_ID=
WCL_CLIENT_SECRET=

BLIZZARD_CLIENT_ID=
BLIZZARD_CLIENT_SECRET=
BLIZZARD_REGION=eu
BLIZZARD_NAMESPACE=profile-classic1x-eu
BLIZZARD_REALM_SLUG=wild-growth

GUILD_ID=811296
PARSE_THRESHOLD=75
WEEKS_REQUIRED=4
WEEKS_WINDOW=8
API_CACHE_SECONDS=10800

# Generate a random one, e.g. `python -c "import secrets;print(secrets.token_urlsafe(50))"`
DJANGO_SECRET_KEY=
DJANGO_DEBUG=0
# Include the server IP (used as the domain) + hostname.
DJANGO_ALLOWED_HOSTS=67.205.160.192,nx01.janeway.systems,localhost,127.0.0.1
